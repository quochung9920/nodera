# Known limitations

Version 0.1.0-alpha.1 is an architectural alpha, not a stable release. The first foundation focuses on persistent identity and the safe external-AI vertical slice. Full professional responsive UI, native Global Styles editing UX, Block Bindings UX, custom Interactivity API blocks, complete visual before/after rendering, browser-matrix E2E evidence and manual accessibility certification remain release gates until implemented and verified.
