# Block contracts

`BlockContractRegistry` projects live WordPress registered block types instead of maintaining a complete duplicate schema. Contracts include registered attributes, supports, parent/ancestor/allowed-child restrictions, context declarations and Nodera AI authorability.

`catalogIndex` is discovery only. AI creation/replacement requires a complete contract and server-side authorability. Unknown third-party and executable-style blocks are conservative by default.
