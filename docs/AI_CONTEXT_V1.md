# nodera-ai-context/v1

A transient projection of the current Gutenberg document for AI authoring. It includes task, immutable target, scope tree, contracts, design/visual context, environment and the required output schema. Editable scope and contextual scope are conceptually separate. Browser geometry must explicitly declare whether it was measured; server code must never fabricate geometry.
